# Agent service (Phase 4 — not yet built)

Will contain a LangGraph state machine:
  intent classification (Instructor) -> RAG node | tool node
Tool nodes reuse the HR/IT/Jira/Zoho agent pattern from prior work.
