# Retrieval service (Phase 2 — not yet built)

Will contain:
- Qdrant hybrid (dense + sparse) collection setup
- bge-reranker-v2 reranking step
- MMR diversity re-ordering
- eval/ scripts using Ragas for Recall@K, context precision
